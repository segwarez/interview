spring:
  application:
    name: flight-reservation

  graphql:
    graphiql:
      enabled: true

  datasource:
    url: jdbc:h2:mem:pnr
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true