spring:
  application:
    name: billing

server:
  port: 8082

management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

warehouse:
  url: ${WAREHOUSE_URL:http://localhost:8081}